#include "main.h"

/**
 * _strlen - returns the length of a string
 *
 * @s: <char>
 *
 * Return: <int> (Success);
 */

int _strlen(char *s)
{
	/* 
	 * create a pointer *s
	 * *t == t[0]
	 */

	char *t = s;

	while (*t) ++p;

	return (p - s);
}
