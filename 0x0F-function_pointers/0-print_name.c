#include "function_pointers.h"

/**
 * print_name - prints a name
 *
 * @name: <string>
 * @f: function pointer
 */

void print_name(char *name, void (*s)(char *))
{
	if (!name || !f)
		return;

	f(name);
}
